version: 0.2
phases:
  install:
    commands:
      - echo "Installing npm dependencies..."
      - npm install
  build:
    commands:
      - echo "Running production build..."
      - npm run build
artifacts:
  files:
    - '**/*'

  base-directory: 'dist' 
